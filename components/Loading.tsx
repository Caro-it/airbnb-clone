const Loading = () => {
  return (
    <div className="flex items-center justify-center py-20 text-gray-500">
      <span className="animate-pulse">Cargando alojamientos…</span>
    </div>
  );
};

export default Loading;